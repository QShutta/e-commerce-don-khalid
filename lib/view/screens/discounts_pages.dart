import 'package:flutter/material.dart';

class DiscountsPages extends StatelessWidget {
  const DiscountsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [Text("This is discount page")],
      ),
    );
  }
}
