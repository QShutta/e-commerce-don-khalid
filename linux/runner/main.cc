#include <iostream>

int main(int argc, char** argv) {
  std::cout << "Starting minimal runner for e_commerce_halfa" << std::endl;
  // The real Flutter runner will be provided by the flutter managed code.
  // This minimal main exists only to satisfy CMake during initial configuration.
  return 0;
}
